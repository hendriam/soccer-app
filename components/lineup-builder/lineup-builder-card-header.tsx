import { CardHeader, CardTitle } from "../ui/card";
export default function LineUpBuilderCardHeader() {
    return (
        <CardHeader>
            <CardTitle className="text-lg font-bold">Lineup Builder</CardTitle>
        </CardHeader>
    );
}
